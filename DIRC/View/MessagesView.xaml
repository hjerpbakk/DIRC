<?xml version="1.0" encoding="UTF-8"?>
<ContentPage 
	xmlns="http://xamarin.com/schemas/2014/forms" 
	xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
	xmlns:local="clr-namespace:DIRC;assembly=DIRC" 
	x:Class="DIRC.View.MessagesView" Title="{Binding Title}">
	<Grid>
		<Grid.RowDefinitions>
			<RowDefinition Height="*" />
			<RowDefinition Height="Auto" />
		</Grid.RowDefinitions>
		<local:CustomListView ItemsSource="{Binding Messages}" SelectedItem="{Binding SelectedMessage}" HasUnevenRows="True">
			<ListView.ItemTemplate>
				<DataTemplate>
					<local:CustomListViewCell Text="{Binding .}" IsMe="false" />
				</DataTemplate>
			</ListView.ItemTemplate>
		</local:CustomListView>
		<StackLayout Grid.Row="1" Padding="10" Orientation="Horizontal">
			<Entry Text="{Binding Message}" HorizontalOptions="FillAndExpand" />
			<Button Text="Send" Command="{Binding SendCommand}" HorizontalOptions="End" />
		</StackLayout>
	</Grid>
</ContentPage>
